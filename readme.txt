STEPS to RUN:

	1) Explode the .tar.gz file
	2) type "make"
	3) Goto 'target' folder
	4) type commands,
		java -jar submissions.jar <Path_To_Docs>
	5) Enter Query when you see "Enter Query:"
	6) Type "Exit" to close the program.